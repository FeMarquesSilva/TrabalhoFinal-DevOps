pipeline {
    agent any
    stages {
        stage('Docker Info') {
            steps {
                script {
                    sh 'docker --version'
                }
            }
        }
    }
}
